# Stage D: Review (fix-pipeline) — Estimates review findings

- Reviewer: Feature - Review agent (Stages C-D template, adaptado para fix-pipeline via FIX_SESSION_DIR)
- Escopo revisado: 6 arquivos backend (apps/api/plane/app/views/estimate/base.py, property.py, urls/estimate.py, views/__init__.py, 2 arquivos de teste) + 11 arquivos frontend (project-estimate.store.ts, estimate-property.service.ts, 7 call sites de getIssueEstimatePropertyValues, estimates/root.tsx, estimates/properties/root.tsx) + .claude/memories/repo/issues-not-fixed.md (novo)
- Fontes lidas antes da revisão: fix-plan.md (revisão 2, aprovado), plan-review.md (rodadas 1 e 2, incl. condição C1), stage-c-fix.md, fix-classifications.md, fix-progress.md, root-cause.md, bug-report.md

## Resultado

**ZERO findings SR-N novos.** Todos os 8 pontos de escrutínio pedidos foram verificados de forma independente, lendo o código-fonte diretamente (não confiando no auto-relato do Fix agent) e, no ponto 8, executando a suíte de testes de fato:

1. **Condição C1 (invalidação de `issueValuesFetchState`)** — VERIFICADA correta. Os 6 mutadores (`updateEstimate:521`, `deleteEstimate:555`, `createEstimateProperty:628`, `updateEstimateProperty:645`, `deleteEstimateProperty:652`, `upsertKpiRoleEstimateProperty:675`) chamam `invalidateEstimatePropertiesCache(projectId)` (`project-estimate.store.ts:700-705`), que limpa `projectPropertiesFetchState[projectId]` E zera `issueValuesFetchState` por inteiro. Reconstitui-se o cenário do B2 (projeto sem estimate → issues "done" sem fetch → admin ativa estimate → volta à lista) e o reset funciona: o próximo `getIssueEstimatePropertyValues` para aquela issue encontra estado "ausente" e refaz o fetch.

2. **Endpoint bulk (F1+F2)** — `IssueEstimatePropertyValueBulkListEndpoint` (`property.py:180-223`) usa `@allow_permission([ROLE.ADMIN, ROLE.MEMBER, ROLE.GUEST])`, idêntico ao `IssueEstimatePropertyValueListEndpoint` (endpoint por-issue). Query única (`filter(...issue_id__in=issue_ids)`), sem N+1: `IssueEstimatePropertyValueSerializer` usa `fields="__all__"` com FKs como `PrimaryKeyRelatedField`, que o DRF resolve via `serializable_value()` (lê a coluna `_id` direto, sem disparar o descriptor de FK) — confirmado zero queries extras por linha. Contrato de resposta (`{issueId: [values]}`) corresponde exatamente ao que `fetchIssueEstimatePropertyValuesBulk` no frontend espera e ao que o coalescer do store consome.

3. **F3 (dedent em estimate/base.py)** — confirmado: `issues.update(...)` e o update de `IssueEstimatePropertyValue` (linhas 448-451) estão corretamente FORA do `for issue in issues:` (421-438), enquanto `issue_activity.delay` permanece DENTRO do loop. `TestEstimatePointDestroyReassignment` cobre exatamente o cenário do bug-report (issue sem `estimate_point` legado, valor só em `IssueEstimatePropertyValue`) com RED→GREEN documentado, mais um teste de edge case com assert de contagem de `issue_activity.delay`.

4. **4 testes reescritos com `str(...)`** — lidos os 3 pontos de uso em `test_estimates_app.py` (linhas ~100, 113, 155, 166, 199, 210) e 1 em `test_estimate_property_values.py` (linha 368): todos corrigem exclusivamente a discrepância UUID-object-vs-str do `response.data` pré-render do DRF; nenhuma asserção de negócio foi enfraquecida ou teve seu tipo esperado trocado por conveniência.

5. **5º teste reescrito** (antes chamava endpoint bulk-delete inexistente, 405) — virou `test_deleting_point_with_no_replacement_nulls_issue_and_property_value_synchronously`, dentro da MESMA classe `TestEstimatePointDestroyNoReplacementSynchronousNulling` que já tinha 2 outros testes (cobertura individual de `Issue.estimate_point` e de `IssueEstimatePropertyValue` respectivamente) — o 3º teste cobre a combinação das duas, via o endpoint real de delete single-point. Não é vazio/trivial: assert de nulling em ambas as tabelas.

6. **Coalescer no frontend** — tracing linha a linha de `getIssueEstimatePropertyValues`/`drainIssueValueFetchQueue`/`failIssueValueFetchBatch` (`project-estimate.store.ts:726-854`) não encontrou race condition real. O único caminho não-óbvio (um 2º caller "in-flight" chegando DEPOIS que o batch já foi detached, mas ANTES do fetch resolver) retorna `undefined` sem esperar — mas isso é inofensivo porque nenhum call site usa o valor de retorno (todos fazem `.catch(() => {})`); os dados chegam via observable reativo do MobX, não via a Promise. Confirmado via grep dos 7 call sites: os 3 de lista (`all-properties.tsx:182`, `estimate-column.tsx:43`, `draft-issue-properties.tsx:72`) usam o default `force=false`; os 4 de issue única (`peek-overview/properties.tsx:85`, `issue-detail/sidebar.tsx:85`, `issue-modal/components/default-properties.tsx:94`, `power-k/.../work-item/root.tsx:55-60`) passam `force: true`. `force` não vaza para nenhum site de lista.

7. **issues-not-fixed.md** — arquivo criado corretamente com o header exato do protocolo, 2 entradas (F8 e órfãos-do-F3) com todos os campos obrigatórios (`**File**`, `**Problem**`, `**Why deferred**`, `**Found in**`, `**Sources**`, `**Last updated**`), ambas autossuficientes (superfície completa de endpoints com linhas verificadas, ressalva de implementação, queries SQL de dimensionamento para quem retomar). Único ponto observado, não reportado como finding por ser puramente cosmético: os IDs em `**Sources**` (`SR-F8`, `SR-F3-orphans`) não seguem o padrão sequencial `SR-NNN` de um findings.md real — mas isso é esperado, já que esta sessão de fix-pipeline partiu de um `fix-plan.md` já aprovado, não de uma `findings.md` gerado por um Review agent (documentado explicitamente no preâmbulo de `fix-classifications.md`).

8. **Regressão do KPI** — rodei pessoalmente `docker exec -w /code plane-api-1 python -m pytest plane/tests -k "kpi or estimate" -q` → **85 passed, 2 failed** (`TestKpiMemberAggregates::test_split_equally_between_two_assignees`, `TestWorkspaceKpiMemberAggregates::test_aggregates_across_all_projects_in_workspace`, ambos com `IntegrityError: duplicate key value violates unique constraint "users_username_key"`). Confirmei via `git stash` (removendo TODAS as mudanças desta sessão) + rerun apenas desses 2 testes: **falham identicamente no código não modificado** — pré-existentes, não-relacionados, confirmados independentemente (não apenas aceita a alegação do Fix agent). `git stash pop` restaurou a árvore de trabalho sem perdas. Também rodei a suíte direta (`test_estimates_app.py` + `test_estimate_property_values.py`) → **35 passed**, confirmando o número do stage-c-fix.md. Rodei `pnpm run check:types` em `apps/web` → 40 erros TS, ZERO relacionados a "estimate" — consistente com a alegação de "mesmos 40 erros antes/depois".

## Verificações adicionais (além dos 8 pontos pedidos)
- `apps/api/plane/app/permissions/project.py`, `apps/api/plane/api/views/estimate.py`, `apps/api/plane/api/urls/estimate.py`, `apps/api/plane/app/views/kpi/issue.py`, `apps/api/plane/db/mixins.py`, `docker-compose-local.yml` — confirmado ZERO diff (git diff --stat vazio): a superfície DEFERRED (F8) não foi tocada, consistente com a decisão do dev.
- `views/__init__.py` — diff puramente aditivo (1 linha, export novo).
- F7 (código morto `handleEstimate`) — confirmado zero referências residuais após remoção (grep vazio).
- F5/F6 (lixeira system-default / picker no create) — lidos `estimates/properties/root.tsx` e `estimates/root.tsx` na íntegra: condições e handlers corretos.

## Conclusão
Nenhum finding SR-N sobrevive a esta rodada de revisão independente. A sessão pode prosseguir diretamente para o próximo stage do fix-pipeline sem passar pelo Fix agent novamente (não há nada para ele classificar/corrigir nesta rodada).

- Status: REVIEW_COMPLETE
- findings.md: 0 entries (nenhum finding novo desde o Stage C)
- Next: Stage E — Sanity (fix-pipeline)
